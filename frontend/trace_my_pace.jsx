import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
    // const store = configureStore();

    //TESTING

    //TESTING

    const root = document.getElementById('root');
    ReactDOM.render(<h1> Page renders </h1>, root);
});